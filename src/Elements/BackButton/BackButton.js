import React from "react";
import "./BackButton.css";

const BackButton = () => {
  return (
    <a href="/" className="back_button">
      Back
    </a>
  );
};

export default BackButton;
